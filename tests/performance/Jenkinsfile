pipeline {
    agent any
    environment {
        SOMETHING = ''
    }
    stages {
        stage('Setup Test Framework') {
            steps {
                git changelog: false, poll: false, url: 'https://github.com/rancher/rancher2-scaling'
            }
	}
	stage('Terraform') {
            steps {
		cd /rancher2-test/control-plane
                sh 'ls'
            }
        }
    }
}
